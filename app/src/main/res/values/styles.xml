<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- GŁÓWNY MOTYW APLIKACJI -->
    <style name="Theme.Waco" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Kolory aplikacji -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Tło aplikacji -->
        <item name="android:colorBackground">@color/white</item>

        <!-- Domyślna czcionka (opcjonalnie) -->
    </style>

    <!-- TWÓJ STYL GRID ITEM -->
    <style name="GridItem">
        <item name="android:orientation">vertical</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_margin">8dp</item>
        <item name="android:padding">16dp</item>
        <item name="android:layout_columnWeight">1</item>
        <item name="android:background">#BBDEFB</item>
        <item name="android:gravity">center</item>
    </style>

    <!-- TWÓJ STYL TEKSTU W GRID -->
    <style name="GridText">
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:textColor">#000000</item>
        <item name="android:textSize">14sp</item>
        <item name="android:gravity">center</item>
    </style>

    <!-- TWÓJ STYL TOOLBARA -->
    <style name="CustomToolbarStyle" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:textColorPrimary">@color/gray</item>
        <item name="android:colorControlNormal">@color/gray</item> <!-- kolor ikony wstecz -->
    </style>

</resources>
